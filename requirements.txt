# Development dependencies for coding practice project
# Install with: pip install -r requirements.txt

# Code quality and formatting
pre-commit>=3.0.0
ruff>=0.4.0
black>=23.0.0

# Note: Runtime dependencies are minimal as this project uses mostly standard library
# Standard library modules used: logging, sys, os, importlib, subprocess
